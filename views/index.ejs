<!DOCTYPE html>
<html>

<head>
  <title>
    <%= title %>
  </title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
</head>

<body class='index' style="background-color: <%=locals.user ? locals.user.color : '#ffffff' %>">

  <% if (locals.user) { %>

    <div class="container p-5">
      <div class="row">
        <h1 class="h1 border border-2 p-4">Hola <%= locals.user.name %>
        </h1>
        <p class="p border border-2 p-3">Elegiste el color 
          <% if (locals.user.color == 'red' ) { %>
            <small style="color: red">Rojo</small>
          <% } else if (locals.user.color == 'green' ) { %>
            <small style="color: green">Verde</small>
          <% } else if (locals.user.color == 'blue' ) { %>
            <small style="color: blue">Azul</small>
          <% } else if (locals.user.color == 'yellow' ) { %>
            <small style="color: yellow">Amarillo</small>
            <%} %>
        </p>
        <p class="p border border-2 p-3">Tu email es: <%= locals.user.email %>
        </p>
        <p class="p border border-2 p-3">Y tu edad es: <%= locals.user.age%>
        </p>
        <a href="/logout">Salir</a>
      </div>
    </div>

    <% } else { %>
      <div class='container'>
        <form action="" method="POST">
          <div class='div-container'>
            <div class="div-children">
              <label for="name">Ingrese su nombre:</label>
              <input type="name" name="name" id="name">
              <span>
                <%= locals.errors && errors.name ? errors.name.msg : null %>
              </span>
            </div>
            <div class="div-children">
              <label for="colors">Elegi un color:</label>
              <select name="color" id="color">
                <option hidden>Selecciona un color</option>
                <option value="blue">Azul</option>
                <option value="yellow">amarillo</option>
                <option value="red">rojo</option>
                <option value="green">verde</option>
              </select>
              <span>
                <%= locals.errors && errors.color ? errors.color.msg : null %>
              </span>
            </div>
            <div class="div-children">
              <label for="email">Ingrese su email:</label>
              <input type="email" name="email" id="email">
              <span>
                <%= locals.errors && errors.email ? errors.email.msg : null %>
              </span>
            </div>
            <div class="div-children">
              <label for="age">Ingrese su edad:</label>
              <input type="number" name="age" id="age">
              <span>
                <%= locals.errors && errors.age ? errors.age.msg : null %>
              </span>
            </div>
            <div class="">
              <label for="recordar">Recordarme</label>
              <input type="checkbox" name="recordar" id="recordar">
            </div>
          </div>
          <div class="div_button">
            <button type="submit">Ingresar</button>
          </div>
          <form>
      </div>
      <% } %>
</body>

</html>